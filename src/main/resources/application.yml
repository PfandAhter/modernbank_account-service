cache:
  refresh:
    cronErrorCode: 0 0 1 * * SUN # every day at 1 AM
    accounts: 0 */5 * * * * # every 5 minutes
  user:
    cleanup:
      rate: 43200000 # 12 hours

spring:
  application:
    name: account-service
  data:
  datasource:
    url: jdbc:mysql://localhost:3306/modern_bank_account_service
    username: springstudent
    password: springstudent
  jpa:
    hibernate:
      ddl-auto: update
  cache:
    type: redis
    redis:
      host: localhost
      port: 6379
  main:
    banner-mode: off
server:
  port: 8084